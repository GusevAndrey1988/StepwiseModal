import ModalOptions from "./ModalOptions";

export default class Modal {
    constructor (
        private name: string,
        private options: ModalOptions,
    ) {
    }

    public show(): void {

    }

    public hide(): void {

    }
}